"use client";

interface PdfToolbarProps {
  comicTitle?: string;
  currentPage: number;
  totalPages: number;
  progress: number;
  isLoading?: boolean;
}

export default function PdfToolbar(_props: PdfToolbarProps) {
  return null;
}

